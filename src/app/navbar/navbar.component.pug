nav.navbar.navbar-expand-lg.navbar-dark.bg-dark
  a.navbar-brand(href='dashboard')
    | Imagifinity
  button.navbar-toggler(type='button', (click)='toggleNavbar()')
    span.navbar-toggler-icon
  .collapse.navbar-collapse([ngClass]="{ 'show': navbarOpen }")
    ul.navbar-nav.mr-auto
      li.nav-item(routerlinkActive='active')
        a.nav-link(routerLink='/dashboard') Dashboard
      li.nav-item(routerlinkActive='active')
        a.nav-link(routerLink='/user-edit') Edit User
      li.nav-item(routerlinkActive='active')
        a.nav-link(routerLink='/reservations') Reservations